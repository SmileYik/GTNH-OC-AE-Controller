return {
    sleep = 10,                     -- 两次执行任务时间隔多少秒
    token = "token",                -- token
    baseUrl = "https://123456",     -- 基础 url
    path = {                        -- 各项数据路径
        task = "/task",             -- 任务数据所在路径
        cpu = "/cpus",              -- cpu
        essentia = "/essentia",     -- 源质
        fluids = "/fluids",         -- 流体
        items = "/items"            -- 物品
    }
}
